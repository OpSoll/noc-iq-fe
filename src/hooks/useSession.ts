// Re-export from the centralized SessionProvider so existing imports continue to work.
export { useSession } from "@/providers/session";
export type { SessionState, SessionUser } from "@/providers/session";
